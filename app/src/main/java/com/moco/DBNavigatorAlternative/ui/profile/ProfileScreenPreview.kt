package com.moco.DBNavigatorAlternative.ui.profile

import androidx.compose.runtime.Composable
import androidx.compose.ui.tooling.preview.Preview

/**
 * Preview-Komponente für die Profilseite.
 * Erlaubt die visuelle Überprüfung des ProfileScreens in der Android Studio Design-Ansicht.
 */
@Preview(showBackground = true)
@Composable
fun ProfileScreenPreview() {
    // Rendert den ProfileScreen in seinem Standardzustand (Login)
    ProfileScreen()
}
